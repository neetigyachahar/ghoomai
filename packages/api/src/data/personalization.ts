import type { UserPersonalization } from "@repo/types";

export const mockPersonalization: UserPersonalization = {
  userId: "demo-user",
  preferredAccommodationType: "homestay",
  budgetTier: "mid",
  notes: "Prefers quiet homestays with mountain views over large resorts.",
};
